/**
 *
 * BrowseFilter
 *
 */
import React, { memo } from 'react';

// import styled from 'styles/styled-components';

interface Props {}

function BrowseFilter(props: Props) {
  return <div></div>;
}

export default memo(BrowseFilter);
